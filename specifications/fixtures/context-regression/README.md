# Context Regression Fixture Skeletons

Owner: milestones 43-45.

These fixtures cover the active context-regression correction track. Entries are skeletons until the owning milestone records commands, fixture data, OS/date/result, and report paths. They must not be cited as passing evidence while their `manifest.json` entries remain `implemented: false`.

Expected fixture groups:

- `context-regression-soft-backlog-v1`: raw-lane soft-trigger fixtures proving `overSoft` uses `rawLaneTokens = softBacklogTokens + protectedTailRawTokens` crossing `softThreshold` while useful eligible backlog exists, `softBacklogTokens` and `softBacklogItemCount` use only eligible unsummarized raw-message rows after the newest active sprig boundary and outside mandatory current-turn/unconsumed protection plus the configurable whole-message fresh tail, active hard-fill uses `activeTokens / hardLimit`, first soft pass selects the whole fitting eligible raw span, oversized source selection reduces message count without truncating a message, summary-covered raw rows are excluded, Dolt does not run soft sprig/bindle condensation or archive-stub creation, stale snapshot diagnostics are recomputed, and local-provider background deferral is content-safe.
- `context-regression-maintenance-summary-quality-v1`: summary-budget and quality fixtures proving `summaryTargetTokens` differs from `summaryGenerationMaxOutputTokens`, dedicated `maintenanceInputBudget` bounds source selection, stale condensation generation caps are removed, deterministic tiny-summary/echo/wrapper/refusal/anchorless gates reject invalid outputs, retry/no-reasoning/aggressive prompt paths record objective status, soft work does not commit deterministic fallback, and hard-pressure fallback status is explicit. `summary-quality-vectors-v1.skeleton.json` lists the minimum content-safe objective vectors.
- `context-regression-v1`: deterministic regression report proving hard context fill uses `activeTokens / hardLimit`, raw-lane soft pressure uses `rawLaneTokens / softThreshold`, raw backlog pressure uses `softBacklogTokens / softThreshold`, protected-tail, fresh-tail, and unconsumed post-current raw tokens are reported, over-soft nonblocking scheduling reports `overSoft = true` and `overHard = false`, per-status evidence includes soft `scheduled`, `started`, `deferred`, `skipped`, `completed`, `canceled`, and `failed`, manual/repair `no_op`, and hard `started`, `completed`, `canceled`, `failed`, and `recovery_required`, deterministic fallback counter mode/version is labeled, provider-capacity deferral uses `provider_capacity_deferred` plus `providerEndpointKeyHash` and nonblocking work flags, post-invocation connection failures use `provider_unavailable`, completed tool output, terminal tool-error bytes, and interrupted errored output remain source-preserved through `lcm_file` without false missing-source, `[object Object]`, or object-stringification errors on the next turn, hard-limit busy status clears by operation finalizer, useful summaries are accepted, tiny summaries are rejected, hard-limit fail-closed behavior is visible, and no stale "Preparing memory" status remains after the agent resumes. `maintenance-status-evidence-v1.skeleton.json` and `long-tool-output-cases-v1.skeleton.json` list the minimum content-safe status and terminal-output cases.

`context-regression-report-schemas-v1.json` defines the exact machine-readable report IDs, status policy, global required fields, field types, `additionalProperties = false`, and content-safe metric fields for release evidence. Required check statuses are `passed`, `failed`, or `blocked`; the deterministic fake/local OpenAI-compatible checks listed in `mandatoryPassIDs` must pass before the report can pass. Every report entry must include `checkID`, `status`, `command`, `os`, and `date`; deterministic passed/failed evidence also records `artifactPath`. Report entries must include the required fields and exact expected values for their check ID, such as `activeTokens`, `hardLimit`, hard/soft ratios, `overSoft`/`overHard`, maintenance status evidence/work flags, normalized `providerOutputLimit`, provider-capacity deferral fields, provider-unavailable safe errors, terminal tool-output source-preservation fields, summary source/candidate/accepted token counts, summary budget/quality fields, safe errors, and stale-status booleans.

Fixtures and reports are content-safe non-model surfaces. They may store IDs, hashes, counts, enum statuses, token counter mode/version, safe errors, and fixture-owned model-visible sample strings. They must not serialize raw user prompts, summaries, rejected provider output, provider reasoning text, tool output, file bytes, raw provider payloads, endpoint URLs, credentials, helper stdout/stderr, or arbitrary exception text.
